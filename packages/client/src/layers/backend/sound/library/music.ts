import { SoundAsset } from ".";

const BASE_VOLUME = 0.3;
const FULL_VOLUME = 1;
export const music: SoundAsset = {
  sailing: {
    src: "/sounds/music/sailing.mp3",
    volume: BASE_VOLUME,
  },
};
